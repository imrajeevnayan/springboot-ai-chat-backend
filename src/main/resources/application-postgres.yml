spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/chat_backend}
    username: ${DB_USERNAME:chat_app}
    password: ${DB_PASSWORD:change-me}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
